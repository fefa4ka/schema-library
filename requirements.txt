# skidl requires KiCad libraries
# git+https://github.com/xesscorp/skidl.git
# skidl installed by Dockerfile without deps. Below dependencies for skidl, widthout GUI
future==0.15.0
kinparse==0.1.0
enum34
graphviz

# libngspice need to be installed for PySpice
PySpice==1.3.2

# web backend
flask-api
peewee==3.8.2

# Experimental
sympy
lcapy
Jetson.GPIO
